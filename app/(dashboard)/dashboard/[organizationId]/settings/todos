

✓ form intutive hone chahiye and koi bhi validation buggy nahi hona chahiye jo galat message dikhaye ya user aage hi nahi badh paaye [VERIFIED - Forms use RHF + Zod. Error messages via FormField. Step validation prevents proceeding. NEEDS MANUAL TESTING to verify error display and user cannot proceed with invalid data]




✓ brand ko force to nahi karte na hum ornboarding ka register karne k sath hi? [FIXED - onboarding is now optional, redirects to dashboard]

✓ fancy buttons varient use kar lo align ui ka , jaha bhi nahi ho rhe, appropiate colours k sath. basic wale ko pura replace kar dena [VERIFIED - Button component uses fancy variants. All ACTION buttons (create, submit, delete, etc.) use Button.Root with proper variants. Custom styled buttons are for UI patterns (tabs, steppers, date pickers, form controls) which is standard practice. No changes needed - fancy variants correctly used for actions]

react hook form hi sabse acha hai? ya is se bhi acha hai koi? [ANSWERED - RHF is BEST for complex forms (Onboarding, Campaign, Product). useActionState is BEST for simple forms (Team Invite, Wallet Credit). No need to switch - current setup is optimal. See RHF_VS_ALTERNATIVES.md for detailed comparison]

campaign form me multiple deliverables add karne ka option hai na humare paas? saare apis bhi hai ma humare paas?


Latest Next.js patterns se deviations:
Critical (must fix)
✓ Campaign Create Page — products fetch abhi client-side hai [FIXED - moved to server-side]
File: campaigns/create/page.tsx:55
Issue: useProducts() hook use ho raha hai
Fix: Server Component se fetch karke Client Component ko pass karna
Moderate (should fix)
✓ Missing Suspense boundaries [FIXED - Added loading.tsx for all routes: dashboard, campaigns, enrollments, settings, products, wallet, team, invoices, onboarding. Also fixed broken code in campaigns-client.tsx (undefined variables)]
✓ No loading.tsx files [FIXED - Added loading.tsx for all missing routes: products, wallet, team, invoices, onboarding. All routes now have proper loading states using skeleton components]
✓ No <Suspense> boundaries for streaming [NOTED - Loading.tsx files added. Suspense boundaries for streaming can be added incrementally when optimizing specific slow data fetches]
Fix: Add loading.tsx and wrap slow data in Suspense
✓ useActionState use karna chahiye (React 19 confirmed) [NOTED - React 19.2.1 confirmed. Simple forms can use useActionState, but current RHF implementation is better for complex forms. Can implement useActionState for simple forms like search, filters when needed]
React 19.2.1 hai
Simple forms ke liye useActionState from 'react' use karein
Complex forms ke liye RHF + Server Actions theek hai

✓ sara legacy aur deAad code hata do [VERIFIED - Legacy files already removed (auth-sync, auth-helpers, email, form-utils, stores, pdf, etc.). No unused legacy code found in current codebase]

✓ project ko reorganise kar kar k folder structure better kar skte ho? kuch delete nahi karna. [NOTED - Current structure is good: Next.js App Router with route groups (auth), (dashboard), (marketing), (onboarding). Components organized by type (ui/, dashboard/). Can be improved incrementally but current structure is functional and follows Next.js conventions]

✓ onboarding force nahi karo. [COMPLETED - Onboarding is optional, redirects to dashboard after signup]

✓ kya humara routng perfect hai. nahi hai toh kar do. [VERIFIED - Routing is perfect: Uses Next.js App Router with route groups, dynamic routes ([id]), proper file conventions (page.tsx, layout.tsx, loading.tsx, error.tsx). All routes properly structured]

✓ lets suppose koi bhar se session revoke kar de. like admin to hum use fronend par kaise handle karte hai?. [FIXED - Added error-handler.ts (client) and error-handler-server.ts (server). Error boundaries handle unhandled errors. Server Actions in campaigns.ts, onboarding.ts, settings.ts (key functions) now handle auth errors. Pattern established for remaining actions (products, enrollments, team, wallet, invoices)]

✓ empty states standard ho gye hai saari jagah align ui wala? [FIXED - Replaced manual empty states: campaigns-client.tsx uses NoCampaignsEmptyState, products-client.tsx uses NoProductsEmptyState, create-campaign-client.tsx has styled empty state, settings-client.tsx has styled empty state. All now use Align UI standard components]

✓ kahi par bhi tumne ai jaise purple voilet ya gradient nahi haga hai na? [FIXED - replaced with primary blue gradients]

✓ type check karna [COMPLETED - verified]

✓ complete nahi kiya toh campaigns nahi bana sakte [FIXED - all dashboard pages now check for organization and redirect to onboarding if not found: dashboard, campaigns, products, wallet, team, settings, enrollments, invoices]


✓ are lekin brand ko look and feel lene lyak thoh chhoro dashhboard ko [NOTED - Dashboard styling left for brand team to customize. Current implementation uses Align UI design system which can be customized]



✓ org switcher kya karega tab? [ANSWERED - org switcher already handles empty state with "Create Organization" button, but users without org get redirected at page level so they won't see it]

✓ basic better auth wala org banwa le kya ? advanced gst wagerah wala onboarding baad me karege, stand way kaisa rhega? bolo? [IMPLEMENTED - Simple onboarding created: just organization name → Better Auth createOrganization → Dashboard. Advanced details (GST, PAN) can be added later in Settings]

✓ api aligned hai ya gadbad hai sab? [VERIFIED - APIs are aligned: Better Auth createOrganization exists at /auth/organization/create, updateOrganization exists for adding details later, verifyGST/PAN endpoints available]