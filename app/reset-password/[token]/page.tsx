/**
 * Dynamic route for password reset: /reset-password/[token]
 * 
 * This handles Better Auth's default password reset URL pattern.
 * Re-exports the same page component from the parent directory.
 */
export { default } from "../page"
