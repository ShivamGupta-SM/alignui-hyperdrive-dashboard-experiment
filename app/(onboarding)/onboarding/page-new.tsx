/**
 * Onboarding Page - Notion Standard
 *
 * This is a simple wrapper that imports the clean onboarding component.
 * Replace the old page.tsx with this file when ready.
 */

import NotionOnboardingPage from "./onboarding-notion"

export default function OnboardingPage() {
  return <NotionOnboardingPage />
}
