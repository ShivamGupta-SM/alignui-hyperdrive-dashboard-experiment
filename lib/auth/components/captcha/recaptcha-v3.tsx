// RecaptchaV3 stub - captcha libraries removed
// This is a noop component since captcha is not used in this app

import type { ReactNode } from 'react'

export function RecaptchaV3({ children }: { children?: ReactNode }) {
  return <>{children}</>
}
