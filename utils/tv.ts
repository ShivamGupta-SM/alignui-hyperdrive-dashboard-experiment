/**
 * Re-export tailwind-variants with custom configuration
 * This provides a centralized place to configure TV defaults
 */
export { tv, type VariantProps, type ClassValue } from 'tailwind-variants';

